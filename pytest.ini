[pytest]
testpaths = tests
pythonpath = .
addopts = -q -m "not integration"
markers =
    integration: live network tests (Open-Meteo); skipped when offline
filterwarnings =
    ignore::DeprecationWarning
